<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/cekaonica_et_search"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        style="@style/EditTextStyle"
        android:background="@drawable/search_edittext"
        android:drawableStart="@drawable/search_icon"
        android:hint="@string/pretrazi"
        android:autofillHints="@string/pretrazi"
        android:padding="5dp"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:layout_marginStart="@dimen/margin_convention"
        android:layout_marginEnd="@dimen/margin_convention"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/cekaonica_rv"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/cekaonica_et_search"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginTop="@dimen/margin_convention"/>

</androidx.constraintlayout.widget.ConstraintLayout>